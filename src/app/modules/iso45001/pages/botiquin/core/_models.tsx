export interface BotiquinResponse {
    _id: string
    sede: string
    numero: number
    area: string
    ubicacion: string
    createdAt: string //TO ISO STRING
    updatedAt: string //TO ISO STRING
}